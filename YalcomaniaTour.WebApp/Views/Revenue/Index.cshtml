@model IEnumerable<YalcomaniaTour.Entities.Revenue>

@{
    ViewBag.Title = "Index";
}

<h2>Gelir listesi</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Explanation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gain)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RevenueDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Explanation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Gain)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RevenueDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RevenueId }, new { @class = "btn btn-success " }) |
            @Html.ActionLink("Details", "Details", new { id=item.RevenueId }, new { @class = "btn btn-info btn-xl" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RevenueId }, new { @class = "btn btn-danger btn-xl" })
        </td>
    </tr>
}

</table>
